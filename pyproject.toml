[project]
name = "api-opportunity-scanner"
version = "0.2.0"
description = "API market opportunity scanner running on Cloudflare Workers with FastAPI"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
